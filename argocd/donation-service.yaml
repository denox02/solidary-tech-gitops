apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: donation-service
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/gabriel-cupertino/tech-challenge-gitops.git' # Lembre-se de validar se o link do seu repo mudou
    targetRevision: main
    path: donation-service
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: solidary-tech
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true